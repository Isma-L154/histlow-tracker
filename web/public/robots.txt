# Everything under /api/ is data for this page to render, not pages to index.
# Crawling it would spend the Steam key's quota returning JSON nobody reads.
User-agent: *
Disallow: /api/

Sitemap: https://howtoachieve.cloudils.com/sitemap.xml
