{
  "name": "histlow-achievements",
  "version": "0.1.0",
  "private": true,
  "description": "Steam achievement browser: every achievement for a game, ordered by how rare it actually is.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types && tsc --noEmit && wrangler deploy --dry-run",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
