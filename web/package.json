{
  "name": "histlow-achievements",
  "version": "0.1.0",
  "private": true,
  "description": "HowToAchieve: every achievement in a Steam game, ordered by how rare it really is, with how each one is earned.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types && tsc --noEmit && wrangler deploy --dry-run",
    "types": "wrangler types",
    "test": "vitest run --no-watch"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "sharp": "^0.35.4",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.0.0"
  }
}
